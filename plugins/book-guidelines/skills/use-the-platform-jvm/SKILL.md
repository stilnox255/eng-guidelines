---
name: use-the-platform-jvm
description: JVM-specific companion to Use the Platform — which JDK, MicroProfile, and Jakarta EE APIs to reach for before a dependency or a framework-parallel mechanism. Use when writing or reviewing Java/Quarkus/MicroProfile code and deciding whether a helper, utility, or dependency is needed. Not for architecture/layering (see the architecture skill) or non-JVM stacks.
---

# Use the Platform — JVM

Binding companion to the always-on Use the Platform policy. When this skill triggers, read
the full text and follow it:

**Read `${CLAUDE_PLUGIN_ROOT}/docs/guidelines/use-the-platform-jvm.md` and apply its rules
alongside the core policy in `use-the-platform.md`.**
