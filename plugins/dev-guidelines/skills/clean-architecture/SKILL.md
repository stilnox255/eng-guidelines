---
name: clean-architecture
description: Architecture principles from Robert C. Martin — the dependency rule, layering, module boundaries, use cases, entities, separating policy from detail, keeping frameworks/DB/UI at the edges. Use when structuring an application's layers, defining module boundaries, placing dependencies, or deciding where business logic lives. Not for naming, formatting, or statement-level code.
---

# Clean Architecture (Martin)

Binding architecture guideline. When this skill triggers, read the full text and follow it:

**Read `${CLAUDE_PLUGIN_ROOT}/docs/guidelines/clean-architecture.md` and apply its rules to the current architecture/structure decision.**

Core lens: dependencies point inward toward policy; business rules independent of frameworks, DB, and UI; boundaries isolate detail from policy.
