---
name: use-the-platform-browser
description: Browser-specific companion to Use the Platform — which web-standard APIs (custom elements, dialog, fetch, Intl, structuredClone, ...) to reach for before a library. Use when writing or reviewing frontend/browser code and deciding whether a helper or dependency is needed. Not for architecture/layering (see the architecture skill) or non-browser stacks.
---

# Use the Platform — Browser

Binding companion to the always-on Use the Platform policy. When this skill triggers, read
the full text and follow it:

**Read `${CLAUDE_PLUGIN_ROOT}/docs/guidelines/use-the-platform-browser.md` and apply its
rules alongside the core policy in `use-the-platform.md`.**
