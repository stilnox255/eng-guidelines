---
name: use-the-platform-dart
description: Dart/Flutter-specific companion to Use the Platform — which SDK APIs and built-in widgets to reach for before a package or a framework-parallel mechanism. Use when writing or reviewing Dart/Flutter code and deciding whether a helper, utility, or dependency is needed. Not for architecture/layering (see the architecture skill) or non-Dart stacks.
---

# Use the Platform — Dart / Flutter

Binding companion to the always-on Use the Platform policy. When this skill triggers, read
the full text and follow it:

**Read `${CLAUDE_PLUGIN_ROOT}/docs/guidelines/use-the-platform-dart.md` and apply its rules
alongside the core policy in `use-the-platform.md`.**
