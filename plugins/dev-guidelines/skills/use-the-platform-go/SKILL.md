---
name: use-the-platform-go
description: Go-specific companion to Use the Platform — which stdlib APIs to reach for before a module or a framework-parallel mechanism. Use when writing or reviewing Go code and deciding whether a helper, utility, or dependency is needed. Not for architecture/layering (see the architecture skill) or non-Go stacks.
---

# Use the Platform — Go

Binding companion to the always-on Use the Platform policy. When this skill triggers, read
the full text and follow it:

**Read `${CLAUDE_PLUGIN_ROOT}/docs/guidelines/use-the-platform-go.md` and apply its rules
alongside the core policy in `use-the-platform.md`.**
