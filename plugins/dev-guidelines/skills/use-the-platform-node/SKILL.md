---
name: use-the-platform-node
description: Node.js-specific companion to Use the Platform — which Node/web-platform built-ins to reach for before a package or a framework-parallel mechanism. Use when writing or reviewing Node.js/NestJS/TypeScript backend code and deciding whether a helper, utility, or dependency is needed. Not for architecture/layering (see the architecture skill) or non-Node stacks.
---

# Use the Platform — Node.js

Binding companion to the always-on Use the Platform policy. When this skill triggers, read
the full text and follow it:

**Read `${CLAUDE_PLUGIN_ROOT}/docs/guidelines/use-the-platform-node.md` and apply its rules
alongside the core policy in `use-the-platform.md`.**
