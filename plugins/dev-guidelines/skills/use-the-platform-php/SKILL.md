---
name: use-the-platform-php
description: PHP-specific companion to Use the Platform — which PHP standard library and bundled-extension APIs to reach for before a Composer package or a framework-parallel mechanism. Use when writing or reviewing PHP/Symfony/Laravel code and deciding whether a helper, utility, or dependency is needed. Not for architecture/layering (see the architecture skill) or non-PHP stacks.
---

# Use the Platform — PHP

Binding companion to the always-on Use the Platform policy. When this skill triggers, read
the full text and follow it:

**Read `${CLAUDE_PLUGIN_ROOT}/docs/guidelines/use-the-platform-php.md` and apply its rules
alongside the core policy in `use-the-platform.md`.**
