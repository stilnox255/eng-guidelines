---
name: use-the-platform-python
description: Python-specific companion to Use the Platform — which stdlib APIs to reach for before a package or a framework-parallel mechanism. Use when writing or reviewing Python (FastAPI/Django/plain) code and deciding whether a helper, utility, or dependency is needed. Not for architecture/layering (see the architecture skill) or non-Python stacks.
---

# Use the Platform — Python

Binding companion to the always-on Use the Platform policy. When this skill triggers, read
the full text and follow it:

**Read `${CLAUDE_PLUGIN_ROOT}/docs/guidelines/use-the-platform-python.md` and apply its
rules alongside the core policy in `use-the-platform.md`.**
