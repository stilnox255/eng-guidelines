---
name: enterprise-application-patterns
description: Enterprise application patterns from Martin Fowler (PoEAA) — domain logic patterns (Transaction Script, Domain Model, Service Layer), data source patterns (Repository, Data Mapper, Active Record, Unit of Work, Identity Map), object-relational mapping, distribution and concurrency. Use when designing the persistence layer, domain model, ORM mapping, or choosing a data-access pattern. Not for UI or low-level syntax.
---

# Patterns of Enterprise Application Architecture (Fowler)

Binding pattern guideline. When this skill triggers, read the full text and follow it:

**Read `${CLAUDE_PLUGIN_ROOT}/docs/guidelines/patterns-of-enterprise-application-architecture.md` and apply its patterns to the current persistence/domain design.**

Core lens: pick the right domain-logic and data-source pattern for the complexity at hand; manage ORM, identity, and concurrency deliberately.
